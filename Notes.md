📝 Notes
--------

- 🧱 Middleware  
  Add logic that runs before/after each request.


- ❤️ Healthcheck  
  Simple endpoint to check server is up.

- 🔀 API Versioning  
  Prefix all routes under /v1 so future versions can co-exist.